using System.Collections;
using System.Collections.Generic;
using UnityEngine;
namespace Litkey.Quest
{
    public class QuestContributer : MonoBehaviour
    {
        //private QuestManager questManager;

        //private void Start()
        //{
        //    questManager = FindObjectOfType<QuestManager>();

        //}
        //public void Contribute(QuestType questType, string targetId, int amount = 1)
        //{
        //    foreach (var questId in questManager.GetActiveQuestIds())
        //    {
        //        Quest quest = questManager.GetQuest(questId);
        //        if (quest.questType == questType)
        //        {
        //            foreach (var objective in quest.objectives)
        //            {
        //                if (objective.targetId == targetId)
        //                {
        //                    QuestEvents.ReportProgress(questId, objective.objectiveId, amount);
        //                }
        //            }
        //        }
        //    }
        //}
    }
}
