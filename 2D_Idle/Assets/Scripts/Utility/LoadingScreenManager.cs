using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
using TransitionsPlus;

public class LoadingScreenManager : MonoBehaviour
{
    [SerializeField] private TransitionAnimator transition2Battle;
    [SerializeField] private TransitionAnimator transition2Town;

    
    [SerializeField] private Vector2 TransitionWaitTime;
    [SerializeField] private Image LoadingWaitImage;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
